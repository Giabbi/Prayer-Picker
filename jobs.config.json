{
 "jobs": [{
         "functionLocation": "/backend.jsw", 
         "functionName": "resetAbsence",
         "description": "Resets absence after every period", 
         "executionConfig": {
            "time": ["15:25", "16:55", "18:40", "20:45", "22:00"]  // Times are in UTC
         }
     },
     {
         "functionLocation": "/backend.jsw", 
         "functionName": "resetValid",
         "description": "Reset session IDs after an hour", 
         "executionConfig": {
            "cronExpression": "0 * * * *"
         }
     }
 ]
}
